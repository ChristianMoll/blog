{
  "name": "blog",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "set NODE_ENV=dev && next",
    "build": "next build",
    "start": "next start",
    "p-push": "npx prisma db push",
    "p-studio": "npx prisma studio",
    "p-generate": "npx prisma generate",
    "p-migrate": "prisma migrate dev"
  },
  "scriptsComments": {
    "p-push": "Pushes the schema to the db",
    "p-studio": "Open studio webpage for graphical administration of the db",
    "p-generate": "Update after schema change so things like Client receive newest schema",
    "p-migrate": "Generates migration from new schema changes, runs unapplied migrations"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@next-auth/prisma-adapter": "^1.0.4",
    "@prisma/client": "^4.2.1",
    "next": "^12.0.0",
    "next-auth": "^4.10.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-markdown": "8.0.0",
    "sass": "^1.54.6"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/react": "^18.0.0",
    "prisma": "^4.2.1",
    "typescript": "^4.5.5"
  }
}
